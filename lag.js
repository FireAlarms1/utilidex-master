
const Discord = require("discord.js");

module.exports.run = (bot, message, args) => {
  if (command === "lag") {
  message.channel.send("lag my dude!")
};
};

module.exports.help = {
  name: "lag"
}
